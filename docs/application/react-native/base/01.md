# 一、认识​​React Native

## 1.1 介绍

1. 核心定义
​​
    React Native（通常缩写为 RN）是一个由 Facebook（现 Meta）开发的开源框架，用于使用 JavaScript 和 React 来构建真正的、原生的移动应用程序（iOS 和 Android）。​

2. 核心特点与工作原理

    1. 不是 WebView 应用
        
        这是最关键的区别。与 Cordova、Ionic 等框架不同，React Native ​​不运行在 WebView 中​​。你编写的 JavaScript 代码会通过一个“桥接”（Bridge）机制与手机的原生组件（如 UIView、 div）进行通信和交互。

    2. 工作原理：“桥接”机制
        
        你的 JavaScript 代码运行在一个独立的线程中（JavaScript 线程）。当需要更新界面时（例如，改变一个文本内容），它会将一串​​序列化后的消息​​通过“桥接”发送到原生（Native）线程。

3. 传统 Bridge

    - 架构流程图

        ```mermaid
        graph TB
            subgraph JavaScript 线程
                A[JS 代码调用] --> B[Bridge 序列化]
            end
            
            subgraph Native 线程
                C[Bridge 反序列化] --> D[Native 模块处理]
                D --> E[Bridge 序列化响应]
            end
            
            subgraph 通信桥梁
                F[MessageQueue] --> G[BatchedBridge]
                G --> H[异步通信通道]
            end
            
            B --> F
            H --> C
            E --> H
            
            style A fill:#e1f5fe
            style D fill:#f3e5f5
            style F fill:#fff3e0
        ```
    - 详细通信流程

        ```mermaid
        sequenceDiagram
            participant JS as JavaScript
            participant BQ as Bridge Queue
            participant NM as Native Modules
            participant UI as UI 更新

            Note over JS,UI: 异步通信流程
            
            JS->>BQ: 调用 Native 方法 (序列化)
            Note right of JS: 数据被 JSON 序列化
            
            BQ->>NM: 批量传递消息
            NM->>NM: 处理原生逻辑
            
            NM->>BQ: 返回结果 (序列化)
            BQ->>JS: 回调 JavaScript
            
            JS->>UI: 触发 UI 更新
            Note over JS,UI: 整个流程都是异步的
        ```

4. JSI 
    
    - 架构流程图

        ```mermaid
        graph TB
            subgraph JavaScript 运行时
                A[JS 代码调用] --> B[直接 JSI 调用]
            end
            
            subgraph JSI 层
                C[JavaScript 引擎] --> D[JSI 绑定]
                D --> E[Host Objects]
            end
            
            subgraph Native 层
                F[C++ 模块] --> G[同步执行]
                G --> H[直接内存访问]
            end
            
            B --> D
            E --> F
            G --> B
            
            style A fill:#e8f5e8
            style F fill:#fce4ec
            style D fill:#fff3e0
        ```
    - 详细通信流程

        ```mermaid
        sequenceDiagram
            participant JS as JavaScript
            participant JSI as JSI 层
            participant NM as Native 模块
            participant MEM as 共享内存

            Note over JS,MEM: 同步直接调用
            
            JS->>JSI: 调用 JSI 绑定方法
            Note right of JS: 无需序列化
            
            JSI->>NM: 直接方法调用
            NM->>MEM: 读写共享内存
            
            MEM->>NM: 直接数据访问
            NM->>JSI: 同步返回结果
            
            JSI->>JS: 立即返回
            Note over JS,MEM: 同步执行完成
        ```

## 1.2 准备工作

react native 官网 [https://reactnative.dev](https://reactnative.dev)
react native 中文网 [https://reactnative.cn/](https://reactnative.cn/)


1. 安装andriod

    1. 安装node工具

        node 官网 [https://nodejs.org/](https://nodejs.org/zh-cn)

    2. andriod Studio 下载 [https://developer.android.google.cn/studio?hl=zh-cn](https://developer.android.google.cn/studio?hl=zh-cn),确保选中了以下几项：
        - Android SDK
        - Android SDK Platform
        - Android Virtual Device
    3. 当前react native对应的版本(Appearance & Behavior → System Settings → Android SDK)
        - Android 15 

            ![](/application/react-native/001.png)
        - Android SDK Platform 35

            ![](/application/react-native/002.png)
        - Intel x86 Atom_64 System Image（官方模拟器镜像文件，使用非官方模拟器不需要安装此组件）

            >windows Hyper-V一定要启用
    4. 配置 ANDROID_HOME 环境变量

        ![](/application/react-native/003.png)
    5. 把工具目录添加到环境变量 Path

        ![](/application/react-native/004.png)
2. 安装ios环境

    1. 安装 watch 与 node

        ```sh
        brew install node@18
        brew install watchman
        ```
    2. 安装xcode

    3. 安装CocoaPods
        
        ```sh
        brew install cocoapods
        ```

## 1.3 初始化项目

1. 创建项目

    - 初始化项目 

        ```sh
        # 初始化项目
        npx @react-native-community/cli init myproject
        # 加上特定版本
        px @react-native-community/cli init myproject --version X.XX.X
        ```
    - 进入项目

        ```sh
        cd myproject
        ```
    - 运行项目

        - 在package.json中查看脚本

            ```json
            "scripts": {
                "android": "react-native run-android",
                "ios": "react-native run-ios",
                "lint": "eslint .",
                "start": "react-native start",
                "test": "jest"
            },
            ```
        - 运行脚本

            ```sh
            npm run android
            ```
2. 安装 vscode 插件

    ![](/application/react-native/005.png)

3. 调试工具

    - 模拟器调试
        - 模拟器是安装在电脑上的，虚拟的手机界面
        - 模拟器一般跟随 Andriod Studio 和 Xcode 一起安装
        - 启动应用，模拟器会一起启动
    - 真机调试
        - 打开USB调试模式
        - 通过USB线将电脑和手机连起来
        - 启动应用，在手机上安装应用

4. 模拟器调试
    - 点击模拟器(使模拟器获取焦点)
    - 快捷键 ctrl + m
    - 点选 open DevTools
        <div style="display:flex;align-items: center;justify-content: space-between;">
        <image src="/note-front/application/react-native/006.png" width="45%"/> 
        -> 
        <image src="/note-front/application/react-native/007.png" width="45%"/>
        </div>
    - 自动跳转到浏览器控制台
        
        <image src="/note-front/application/react-native/008.png" />
