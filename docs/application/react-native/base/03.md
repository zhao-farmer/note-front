# 三、组件和API

## 3.1 简介

1. RN中的核心组件，是对原生组件的封装

    - 原生组件：Android 或 ios 内的组件
    - 核心组件：RN中最常用的，来自react-native的组件

2. 图形解释

    - RN 组件

        ![](/application/react-native/017.png)
    - RN 组件核心原理

        ![](/application/react-native/018.png)
    - RN 组件列表展示

        ![](/application/react-native/019.png)

3. 核心组件

    | 组件标签 | 组件类型 |
    |------|------|
    | View | 视图组件 |
    | Text | 文本组件 |
    | Alert | 警告框组件 |
    | Button | 按钮组件 |
    | Switch | 开关组件 |
    | StatusBar | 状态栏组件 |
    | ActivityIndicator | 加载指示器组件 |
    | Image | 图片组件 |
    | TextInput | 输入框组件 |
    | Touchable | 触碰组件（共三个） |
    | ScrollView | 滚动视图组件 |
    | SectionList | 分组列表组件 |
    | FlatList | 高性能列表组件 |
    | Animated | 动画组件 |

## 3.2 核心组件

### 3.2.1 按钮与警告框

1. 代码

    ```tsx
    import { StyleSheet, View,Alert,Button } from 'react-native'
    import React from 'react'

    function createTwoButtonAlert(){
        Alert.alert('警告标题','警告内容',[{
            text:'取消',
            onPress:()=>console.log('Cancel Pressed'),
            style:'cancel'
        },{
            text:'确认',
            onPress:()=>console.log('OK Pressed')
        }])
    }
    function createThreeButtonAlert(){
        Alert.alert('更新提示','发现新版本，是否现在更新',[{
            text:'取消',
            onPress:()=>console.log('Cancel Pressed'),
            style:'cancel'
        },{
            text:'确认',
            onPress:()=>console.log('OK Pressed')
        },{
            text:'稍后再试',
            onPress:()=>console.log('稍后提醒我')
        }])
    }

    export default function index() {
        return (
            <View style={[styles.container]}>
                <Button title='Alert.alert' color='red' onPress={()=>{
                    Alert.alert('标题','我是一个按钮')   
                }} />
                <Button title='双个按钮' color='blue' onPress={createTwoButtonAlert} />
                <Button title='三个按钮' color='green' onPress={createThreeButtonAlert} />
            </View>
        )
    }

    const styles = StyleSheet.create({
        container:{ 
            height:800,
            justifyContent:'space-around',
            alignItems:'center',
        }
    })
    ```
2. 运行结果

    ![](/application/react-native/020.gif)

### 3.2.2 Switch 和 StatusBar

1. 代码

    ```tsx
    import { StyleSheet, Switch, View, StatusBar } from 'react-native';
    import { useState } from 'react';

    export default function Index() {
        const [state, setState] = useState({ hiheStatusBar: false });

        return (
            <View style={[styles.container]}>
                <StatusBar
                    hidden={false}
                    backgroundColor="blue" // 仅在Android 应用有效
                    barStyle={'dark-content'} // light-content dark-content
                />
                <Switch
                    trackColor={{ false: '#999', true: '#666' }} // 背景色
                    thumbColor={state.hiheStatusBar ? "red" : "white"} // 前景色 滑动按钮
                    value={state.hiheStatusBar} // 开关状态
                    onValueChange={value => setState({ hiheStatusBar: value })} // 切换开关事件
                />
            </View>
        );
    }

    const styles = StyleSheet.create({
        container: {
            flex: 1,
            justifyContent: 'center',
            alignItems: 'center',
        },
    });

    ```
2. 运行结果

    ![](/application/react-native/021.png)

### 3.2.3 ActivityIndicator


1. 代码

    ```tsx
    import { StyleSheet, View, ActivityIndicator,Platform, Alert } from 'react-native';

    export default function index() {

        // 判断当前平台
        if(Platform.OS==='android'){
            Alert.alert("当前是安卓平台")
        }else{
            Alert.alert("当前是IOS平台")
        }

        return (
            <View style={[styles.container]}>
                <ActivityIndicator color={'blue'} size={'large'} />
                <ActivityIndicator color={'green'} size={'small'} />
                {/* 数字指定大小，只在andriod下有效 */}
                <ActivityIndicator color={'#00d0ff'} size={70} />
                <ActivityIndicator color={'red'} size={100} />
            </View>
        );
    }

    const styles = StyleSheet.create({
        container: {
            flex: 1,
            justifyContent: 'space-around',
        },
    });


    ```
2. 运行结果

    ![](/application/react-native/022.gif)

### 3.2.4 Image组件

1. 介绍

    - 作用：加载图片
    - 加载方式
        - 本地路径
        - 图片的URI地址
        - 图片的Base64字符串
2. 代码示例

    ```tsx
    import { StyleSheet, View,Image,Dimensions } from 'react-native'
    import React from 'react'

    export default function index() {
        return (
            <View style={[styles.container]}>
            <Image
                style={[styles.ItemImage]}
                source={require('./images/meinv.jpg')}
            />
            <Image
                style={styles.tinyLogo}
                source={{
                uri: 'https://reactnative.dev/img/tiny_logo.png',
                }}
            />
            <Image
                style={styles.logo}
                source={{
                uri: 'data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAADMAAAAzCAYAAAA6oTAqAAAAEXRFWHRTb2Z0d2FyZQBwbmdjcnVzaEB1SfMAAABQSURBVGje7dSxCQBACARB+2/ab8BEeQNhFi6WSYzYLYudDQYGBgYGBgYGBgYGBgYGBgZmcvDqYGBgmhivGQYGBgYGBgYGBgYGBgYGBgbmQw+P/eMrC5UTVAAAAABJRU5ErkJggg==',
                }}
            />
            </View>
        )
    }

    const styles = StyleSheet.create({
        container: {
            flex:1,
            justifyContent:'center',
            alignItems:'center'
        },
        ItemImage: {
            height: 200,
            width: Dimensions.get('window').width ,
            marginVertical:20,
        },
        tinyLogo: {
            width: 50,
            height: 50,
        },
        logo: {
            width: 66,
            height: 58,
        },
    })
    ```
3. 运行结果

    ![](/application/react-native/023.png)

### 3.2.5 TextInput组件

1. 代码

    ```tsx
    import { StyleSheet, View,TextInput,Dimensions,Button, Alert } from 'react-native'
    import React, { useState } from 'react'

    function doLogin(text:{username:string,password:string}) {
        Alert.alert('账户信息',`用户名：${text.username}\n密码：${text.password}`)
    }

    export default function Index() {

        const [text,setText] = useState({username:'',password:''})
        return (
            <View style={[styles.container]}>
                <TextInput
                    style={[styles.input]}
                    placeholder='请输入用户名'
                    value={text.username}
                    onChangeText={val=>setText(prev=>({...prev,username:val}))}
                />
                <TextInput
                    style={[styles.input]}
                    placeholder='请输入密码'
                    secureTextEntry={true}  // 密码输入框
                    value={text.password}
                    onChangeText={val=>setText(prev=>({...prev,password:val}))}
                />
                <TextInput
                    style={[styles.input]}
                    placeholder='手机号'
                    keyboardType='numeric'  // 数字键盘
                    maxLength={13}          // 最大长度
                />
                <TextInput
                    style={[styles.input,{height:100}]} // 必须设置高度，否则无法显示多行
                    placeholder='请输入自我介绍'
                    editable
                    multiline               // 多行输入
                    numberOfLines={5}      // 显示行数
                    textAlignVertical='top' // 多行输入时，文字在顶部显示，ios默认顶部，安卓默认第二行
                />
                <View style={[styles.btn]}>
                    <Button  
                        title='登录' 
                        onPress={()=>{doLogin(text)}}
                    />
                </View>
            </View>
        )
    }

    const styles = StyleSheet.create({
        container: {
            flex:1,
            justifyContent:'center',
        },
        input: {
            width: Dimensions.get('window').width - 20 ,
            margin:10,
            borderColor:'red',
            borderWidth:1,
            paddingHorizontal:5,
        },
        btn: {
            padding:10
        }
    })
    ```
2. 运行结果

    ![](/application/react-native/024.gif)

### 3.2.6 Touchable 组件

1. 介绍

    - TouchableHighlight 触碰后，高亮显示
    - TouchableOpactity 触碰后，透明度降低
    - TouchableWithoutFeedback 触碰后，无任何响应

2. 代码示例

    ```tsx
    import {
        StyleSheet,
        Text,
        View,
        TouchableHighlight,
        TouchableOpacity,
        TouchableWithoutFeedback,
    } from 'react-native';
    import React from 'react';

    export default function index() {
        return (
            <View style={[styles.container]}>
                <TouchableHighlight onPress={() => console.log('触碰高亮显示')}>
                    <View style={[styles.item]}>
                        <Text>触碰高亮</Text>
                    </View>
                </TouchableHighlight>
                <TouchableOpacity onPress={() => console.log('触碰透明度变化')}>
                    <View style={[styles.item]}>
                        <Text>触碰透明度变化</Text>
                    </View>
                </TouchableOpacity>
                <TouchableWithoutFeedback onPress={() => console.log('触碰无响应')}>
                    <View style={[styles.item]}>
                        <Text>触碰无响应</Text>
                    </View>
                </TouchableWithoutFeedback>
            </View>
        );
    }

    const styles = StyleSheet.create({
        container: {
            flex: 1,
            justifyContent: 'center',
            alignItems: 'center',
        },
        item: {
            marginBottom: 20,
            padding: 10,
            borderWidth: 1,
            borderColor: 'red',
        },
    });
    ```
3. 运行结果

    ![](/application/react-native/025.gif)

### 3.2.7 ScrollView 与 SafeAreaView

1. 代码示例

    ```tsx
    import { StyleSheet, Text, ScrollView, View, Platform } from 'react-native'
    // SafeAreaView 解决 iPhone X 及以上版本的刘海屏问题
    import { SafeAreaView } from 'react-native-safe-area-context';

    export default function index() {
        return (
            <SafeAreaView>
                {/* 水平滚动 */}
                <ScrollView
                    style={[styles.scrollView2]}
                    horizontal={true} //水平滚动
                >
                    <Text style={[styles.nav]}>新闻</Text>
                    <Text style={[styles.nav]}>娱乐</Text>
                    <Text style={[styles.nav]}>体育</Text>
                    <Text style={[styles.nav]}>财经</Text>
                    <Text style={[styles.nav]}>军事</Text>
                    <Text style={[styles.nav]}>新闻</Text>
                    <Text style={[styles.nav]}>科技</Text>
                </ScrollView>
                {/* 垂直滚动 */}
                <ScrollView 
                    style={[styles.scrollView1]}
                    contentContainerStyle={{margin:30}} //可以设置内容容器样式
                    showsVerticalScrollIndicator={false} //隐藏垂直滚动条
                >
                <Text style={[styles.text]}>
                    Lorem ipsum, dolor sit amet consectetur adipisicing elit. Totam recusandae cum mollitia porro ea beatae amet inventore ratione eius nam fugit cumque illo aliquam, rem minima incidunt deserunt sapiente. Dicta earum laborum officiis porro quaerat molestias error at quibusdam esse eius, magni, aspernatur animi nihil expedita fugiat! In sequi rem ipsum iusto sunt quisquam qui, corporis rerum? Ipsam ad natus vel at, delectus, aperiam eveniet pariatur asperiores laborum mollitia ratione. Quod, placeat perferendis? Libero nulla quam quasi eius, harum illum enim? Excepturi veritatis praesentium aspernatur, atque tempora quaerat, obcaecati eum placeat labore iste quibusdam itaque delectus illum quis architecto animi?
                    {/* 解决 ScrollView 在 Andriod 滚动不到底的问题 */}
                    <View style={{height:Platform.OS === 'ios' ? 0:100}}></View>
                </Text>
                </ScrollView>
            </SafeAreaView>
        )
    }

    const styles = StyleSheet.create({
        text:{
            fontSize:30,
        },
        scrollView1:{
            backgroundColor:'#ccc',
            marginHorizontal:20,
        },
        scrollView2:{
            backgroundColor:'#dfb',
        },
        nav:{
            margin:10,
            height:50,
            fontSize:30
        }
    })
    ```
2. 运行结果

    ![](/application/react-native/026.gif)

### 3.2.8 SelectList组件 (重点)

1. 代码示例

    ```tsx
    import { useState } from 'react';
    import {
        StyleSheet,
        Text,
        View,
        SectionList,
        StatusBar,
        Alert,
    } from 'react-native';
    import { SafeAreaView } from 'react-native-safe-area-context';

    const DATA = [
        {
            title: '魏国',
            data: ['曹操', '司马懿', '张辽'],
        },
        {
            title: '蜀国',
            data: ['刘备', '关羽', '张飞'],
        },
        {
            title: '吴国',
            data: ['孙权', '周瑜', '吕蒙'],
        },
        {
            title: '群雄',
            data: ['张角', '袁绍', '董卓'],
        },
    ];


    export default function Index() {
        // 开启加载动画
        const [isFresh, setIsFresh] = useState(false);
        return (
            <SafeAreaView style={styles.container}>
            <SectionList
                sections={DATA} // 数据源
                keyExtractor={(item, index) => item + index} // key
                // 每一行的渲染
                renderItem={({ item }) => (
                    <View style={styles.item}>
                        <Text style={styles.title}>{item}</Text>
                    </View>
                )}
                // 每个分组头的渲染
                renderSectionHeader={({ section: { title } }) => (
                    <Text style={styles.header}>{title}</Text>
                )}
                // 每一行的分割线
                ItemSeparatorComponent={() => (
                    <View style={{ borderBottomWidth: 1, backgroundColor: 'red' }} />
                )}
                // 列表数据为空时展示的组件
                ListEmptyComponent={() => {
                    return <Text style={{ fontSize: 30 }}>空空如也！</Text>;
                }}
                // 下拉刷新
                refreshing={isFresh} // 是否正在刷新
                onRefresh={()=>{
                    setIsFresh(true); // 开启加载动画

                    setTimeout(() => {
                        setIsFresh(false); // 关闭加载动画
                        Alert.alert('刷新完成！');
                    }, 2000);
                }}  // 下拉刷新触发的事件

                // 上拉刷新
                onEndReachedThreshold={0.1} // 距离底部多少触发上拉加载 0.1 代表10%
                onEndReached={()=>{
                    Alert.alert('加载更多！');
                }}

                // 头部组件
                ListHeaderComponent={()=>{
                    return <Text style={{fontSize:40}}>三国人物</Text>
                }}
                // 尾部组件
                ListFooterComponent={()=>{
                    return <Text style={{fontSize:30, textAlign:'center'}}>我是有底线的！</Text>
                }}
            />
            </SafeAreaView>
        );
    }

    const styles = StyleSheet.create({
        container: {
            flex: 1,
            paddingTop: StatusBar.currentHeight,
            marginHorizontal: 16,
        },
        item: {
            backgroundColor: '#f9c2ff',
            padding: 20,
            marginVertical: 8,
        },
        header: {
            fontSize: 32,
            backgroundColor: '#fff',
        },
        title: {
            fontSize: 24,
        },
    });
    ```
2. 运行结果

    ![](/application/react-native/027.gif)

### 3.2.9 FlatList 组件（重点）

1. 代码示例

    ```tsx
    import {
        StyleSheet,
        Text,
        View,
        FlatList,
        StatusBar,
        TouchableOpacity,
    } from 'react-native';
    import { SafeAreaView } from 'react-native-safe-area-context';
    import { useState } from 'react';

    const state = {
        isLoading: false,
        list: [
            { id: '1', title: '头条' },
            { id: '2', title: '娱乐' },
            { id: '3', title: '体育' },
            { id: '4', title: '科技' },
            { id: '5', title: '汽车' },
            { id: '6', title: '财经' },
            { id: '7', title: '军事' },
            { id: '8', title: '国际' },
            { id: '9', title: '时尚' },
            { id: '10', title: '电影' },
            { id: '11', title: '电视剧' },
            { id: '12', title: '综艺' },
        ],
    };

    const renderItem = ({ item }: any) => {
        return (
            <View style={styles.item}>
            <Text style={styles.title}>{item.title}</Text>
            </View>
        );
    };

    export default function Index() {
        const [selectedId, setSelectedId] = useState('');
        return (
            <SafeAreaView style={styles.container}>
            <FlatList
                data={state.list} // 数据源
                // 每一行的渲染
                renderItem={({ item }) => {
                    let backgroundColor = item.id === selectedId ? '#dfb' : '#f9c2ff';
                    return (
                        <TouchableOpacity
                            style={[styles.item, { backgroundColor }]}
                            onPress={() => {
                                setSelectedId(item.id); // 更新选中项
                            }}
                        >
                            <Text style={styles.title}>{item.title}</Text>
                        </TouchableOpacity>
                    );
                }} 
                // renderItem={renderItem}
                keyExtractor={item => item.id} // key
                // 新的属性
                horizontal={false} // 默认是false，表示垂直布局，设置为true表示水平布局
                initialScrollIndex={0} // 初始滚动到第几个位置
                initialNumToRender={4} // 初始渲染数据的数量，一般数量要填满一屏幕
                numColumns={1} // 指定列数，数据必须等高 - 无法支持瀑布流
                inverted={false} // 列表旋转
                extraData={selectedId} // 额外的刷新数据，一般是选中项

                // 每一行的分割线
                ItemSeparatorComponent={() => (
                    <View style={{ borderBottomWidth: 1, backgroundColor: 'red' }} />
                )} 
                // 列表数据为空时展示的组件
                ListEmptyComponent={() => (
                    <Text style={{ fontSize: 30 }}>空空如也！</Text>
                )} 
                
                // 头部组件
                ListHeaderComponent={() => {
                    return <Text style={{ fontSize: 40 }}>新闻列表</Text>;
                }}
                // 尾部组件
                ListFooterComponent={() => {
                    return (
                        <Text style={{ fontSize: 30, textAlign: 'center' }}>到底了</Text>
                    );
                }}
            />
            </SafeAreaView>
        );
    }

    const styles = StyleSheet.create({
        container: {
            flex: 1,
            paddingTop: StatusBar.currentHeight,
            marginHorizontal: 16,
        },
        item: {
            backgroundColor: '#f9c2ff',
            padding: 20,
            marginVertical: 8,
        },
        header: {
            fontSize: 32,
            backgroundColor: '#fff',
        },
        title: {
            fontSize: 24,
        },
    });
    ```
2. 运行结果

    ![](/application/react-native/028.gif)

### 3.2.10 Animated

1. RN中可以直接使用的动画组件

    - Animated.View
    - Animated.Text
    - Animated.ScrollView
    - Animated.Image

2. 如何创建动画（步骤）
    - 创建初始值
        - Animated.Value() 单个值
        - Animated.ValueXY() 向量值
    - 将初始值绑定的动画组件上

        一般将其绑定到某个样式属性下，例如：opacity,translate
    - 通过动画类型API,一帧一帧地更改初始值

        - Animated.decay() 加速效果
        - Animated.spring() 弹跳效果
        - Animated.timing() 时间渐变效果

3. 示例1：淡变展示

    - 代码

        ```tsx
        import React, { useRef } from 'react';
        import { SafeAreaView } from 'react-native-safe-area-context';
        import { Animated, Text, View, StyleSheet, Button } from 'react-native';

        export default function Index() {
            // fadeAnim将用作不透明度的值。初始值：0
            const fadeAnim = useRef(new Animated.Value(0)).current;

            const fadeIn = () => {
                console.log('fadeIn - start');

                // 将在5秒内将fadeAnim值更改为1
                Animated.timing(fadeAnim, {
                    toValue: 1, // 目标值
                    duration: 5000, // 动画时间
                    useNativeDriver: true, // 使用原生方式渲染动画
                }).start(() => {
                    console.log('fadeIn - end');
                }); // 开始执行动画
            };

            const fadeOut = () => {
                // 将在3秒内将fadeAnim值更改为0
                Animated.timing(fadeAnim, {
                    toValue: 0,
                    duration: 3000,
                    useNativeDriver: true,
                }).start();
            };

            return (
                <SafeAreaView style={styles.container}>
                    <Animated.View
                        style={[
                        styles.fadingContainer,
                        {
                            // 将不透明度绑定到动画值
                            opacity: fadeAnim,
                        },
                        ]}
                    >
                        <Text style={styles.fadingText}>正在淡变地视图!</Text>
                    </Animated.View>
                    <View style={styles.buttonRow}>
                        <Button title="淡变显示" onPress={fadeIn} />
                        <Button title="淡变隐藏" onPress={fadeOut} />
                    </View>
                </SafeAreaView>
            );
        }

        const styles = StyleSheet.create({
            container: {
                height: '100%',
                flex: 1,
                alignItems: 'center',
                justifyContent: 'center',
            },
            fadingContainer: {
                padding: 20,
                backgroundColor: 'powderblue',
            },
            fadingText: {
                fontSize: 28,
            },
            buttonRow: {
                flexBasis: 100,
                justifyContent: 'space-evenly',
                marginVertical: 16,
            },
        });
        ```

    - 运行结果

        ![](/application/react-native/029.gif)

4. 示例2：扫描图案

    - 代码示例

        ```tsx
        import React, { useRef } from 'react';
        import { SafeAreaView } from 'react-native-safe-area-context';
        import { Animated,  View, StyleSheet } from 'react-native';

        export default function Index() {
        
            const moveAnim = useRef(new Animated.Value(0)).current;

            // 模拟声明周期函数
            React.useEffect(() => {
                Animated.loop(
                Animated.timing(moveAnim, {
                    toValue: 200, // 目标值
                    duration: 2000, // 动画时间
                    useNativeDriver: true, // 使用原生方式渲染动画
                }),
                ).start();
            }, [moveAnim]);

            return (
                <SafeAreaView style={styles.container}>
                    <View style={[styles.scanContainer]}>
                        <Animated.View
                        style={[
                            styles.border,
                            {
                            transform: [
                                {
                                translateY: moveAnim,
                                },
                            ],
                            },
                        ]}
                        />
                    </View>
                </SafeAreaView>
            );
        }

        const styles = StyleSheet.create({
            container: {
                height: '100%',
                flex: 1,
                alignItems: 'center',
                justifyContent: 'center',
            },
            scanContainer: {
                width: 200,
                height: 200,
                borderWidth: 1,
                borderColor: 'green',
            },
            border: {
                borderWidth: 1,
                borderColor: 'red',
            },
        });
        ```
    - 运行结果

        ![](/application/react-native/030.gif)

## 3.3 第三方组件


### 3.3.1 使用方式

1. 介绍

    - 需要单独安装组件
    - 使用步骤
        - 安装
        - 配置
        - 使用

2. 常用组件

    | 组件名称         | 组件作用     |
    |--------------|----------|
    | WebView      | 相当于内置浏览器 |
    | Picker       | 下拉框      |
    | Swiper       | 展示轮播效果   |
    | AsyncStorage | 持久化存储系统  |
    | Geolocation  | 获取定位信息   |
    | Camera       | 调用摄像头    |

### 3.3.2 WebView (内置浏览器)

1. 组件使用

    - 安装

        ```sh
        npm install --save react-native-webview
        ```
    - 配置

        [https://github.com/react-native-webview/react-native-webview](https://github.com/react-native-webview/react-native-webview)
    - 使用
        - 方法1：直接指定uri地址
        - 方法2：直接渲染html代码

2. URI示例

    - 代码

        ```tsx
        import React, { Component } from 'react';
        import { WebView } from 'react-native-webview';

        class MyWeb extends Component {
            render() {
                return (
                    <WebView
                        source={{ uri: 'https://www.baidu.com' }}
                        style={{ marginTop: 20 }}
                    />
                );
            }
        }
        export default MyWeb;
        ```
    - 运行结果

        ![](/application/react-native/031.png)

3. HTML示例

    - 代码

        ```tsx
        import React, { Component } from 'react';
        import { WebView } from 'react-native-webview';

        class MyInlineWeb extends Component {
            render() {
                return (
                    <WebView
                        originWhitelist={['*']}
                        source={{ html: '<h1 style="color:red">Hello world</h1>' }}
                    />
                );
            }
        }

        export default MyInlineWeb;
        ```
    - 运行结果

        ![](/application/react-native/032.png)

### 3.3.3 Picker （弹窗）

1. 组件使用
    - 安装

        ```sh
        npm install @react-native-picker/picker --save
        ```
    - 配置
        - [https://github.com/react-native-picker/picker](https://github.com/react-native-picker/picker)
        - 注意不同版本不同配置
    - 使用
       
       注意平台之间地差异（Android|IOS）

2. 代码示例

    ```tsx
    import { View, StyleSheet } from 'react-native';
    import { useState } from 'react';
    import { Picker } from '@react-native-picker/picker';

    export default function Index() {
        const [state, setState] = useState({ color: 'white' });

        return (
            <View style={[styles.container, { backgroundColor: state.color }]}>
            <Picker
                selectedValue={state.color}
                style={[styles.box]}
                // mode={'dropdown'} // 只在安卓生效
                onValueChange={(itemValue, _) => setState({ color: itemValue })}
            >
                <Picker.Item label="白色" value="white" />
                <Picker.Item label="红色" value="red" />
            </Picker>
            </View>
        );
    }

    const styles = StyleSheet.create({
        container: {
            flex: 1,
            justifyContent: 'center',
            alignItems: 'center',
        },
        box: {
            height: 50,
            width: 100,
        },
    });
    ```
3. 运行结果

    ![](/application/react-native/033.gif)

### 3.3.4 Swiper (轮播效果)

1. 组件使用

    - 安装

        ```sh
        npm i react-native-swiper --save
        ```
    - 使用

        [https://github.com/leecade/react-native-swiper](https://github.com/leecade/react-native-swiper)

2. 代码示例

    ```tsx
    import { StyleSheet, ScrollView, Image, Dimensions } from 'react-native';
    import Swiper from 'react-native-swiper';

    export default function index() {
        return (
            // 因为轮播图超出了屏幕范围，所以需要用ScrollView包裹
            <ScrollView>
                <Swiper
                    style={[styles.wrapper]}
                    showsButtons={true} // 是否显示左右按钮
                    autoplay={true} // 是否自动轮播
                    autoplayTimeout={2} // 自动轮播间隔时间
                    showsPagination={true} // 是否显示指示点
                >
                    <Image style={[styles.slideImage]} source={require('./images/1.jpg')} />
                    <Image style={[styles.slideImage]} source={require('./images/2.jpg')} />
                    <Image style={[styles.slideImage]} source={require('./images/3.jpg')} />
                </Swiper>
            </ScrollView>
        );
    }

    const styles = StyleSheet.create({
        wrapper: {
            height: 200,
        },
        slideImage: {
            height: 200,
            width: Dimensions.get('window').width,
            resizeMode: 'cover',
        },
    });
    ```
3. 运行结果

    ![](/application/react-native/034.gif)

### 3.3.5 AyncStorage key-value存储

#### 基础使用

1. 组件使用

    - 安装

        ```sh
        npm install @react-native-async-storage/async-storage
        ```
    - 配置

        [https://github.com/react-native-async-storage/async-storage](https://github.com/react-native-async-storage/async-storage)
    - 使用

        增删改查

2. 代码示例

    ```tsx
    import { StyleSheet, View,Button, Alert } from 'react-native'
    import AsyncStorage from '@react-native-async-storage/async-storage';

    const storeData = async(value:string) =>{
        try {
            await AsyncStorage.setItem("mytest",value)
            Alert.alert("存储成功")
        } catch (error) {
            
        }
    }

    const getData = async() =>{
        try {
            const value = await AsyncStorage.getItem("mytest")
            if(value !== null){
                Alert.alert("存储地值：" + value)
            }
        } catch (error) {
            
        }
    }

    export default function index() {
        return (
            <View style={[styles.container]}>
                <Button title='存储' onPress={()=>(storeData("Hello RN"))}/>
                <Button title='获取' onPress={getData}/>
            </View>
        )
    }

    const styles = StyleSheet.create({
        container:{
            flex:1,
            justifyContent:"center",
            alignItems:"center",
            gap:10
        }
    })
    ```
3. 运行结果

    ![](/application/react-native/035.gif)

#### 封装 AsyncStorage

1. 组件使用

    - 增 set(key,value)
    - 删 delete(key) | clear()
    - 改 update(key,value)  `主要考虑value 是对象`
    - 查 get(key)

2. 代码示例

    - storage.ts
        ```ts
        import AsyncStorage from '@react-native-async-storage/async-storage';

        class Storage{
            // 添加数据
            static set(key:string,value: number | string | object ){
                return AsyncStorage.setItem(key,JSON.stringify(value))
            }

            // 获取数据
            static get(key:string){
                return AsyncStorage.getItem(key).then(value=>{
                    if (value && value !== '') {
                        const jsonValue = JSON.parse(value)
                        return jsonValue;
                    }
                }).catch(()=>null)
            }

            // 更新数据
            static update(key:string,newValue: number | string | object){
                return AsyncStorage.getItem(key).then(oldValue =>{
                    newValue = typeof newValue === 'string' ? newValue : Object.assign({},oldValue,newValue);
                    return AsyncStorage.setItem(key,JSON.stringify(newValue))
                })
            }

            // 删除指定的key
            static delete(key:string){
                return AsyncStorage.removeItem(key);
            }

            // 清空所有数据 
            static clear(){
                return AsyncStorage.clear()
            }
        }

        export default Storage
        ```
    - useStorage.tsx

        ```tsx
        import { StyleSheet, Button, View,Alert } from 'react-native'
        import React from 'react'
        import Storage from './storage'

        const getMyData = async() =>{
            try {
                const value =  await Storage.get("mydata")
                if(value !== null){
                    Alert.alert("存储的值：" + value)
                }
            } catch (error) {
                
            }
        
        }

        export default function useStorage() {
            return (
                <View style={[styles.container]}>
                    <Button title='存储' onPress={ async()=>( await Storage.set("mydata","Hello Storage"))}/>
                    <Button title='获取' onPress={ getMyData}/>
                    <Button title='清空' onPress={Storage.clear}/>
                </View>
            )
        }

        const styles = StyleSheet.create({
            container:{
                flex:1,
                justifyContent:"center",
                alignItems:"center",
                gap:10
            }
        })
        ```

3. 运行结果

    ![](/application/react-native/036.gif)

### 3.3.6 Geolocation 定位

目前国内无法获取 谷歌的 andriod的经纬度 信息，也能用阿里的，不是官方也不更新了，如果软件需要定位
不建议使用react native

[https://github.com/qiuxiang/react-native-amap-geolocation](https://github.com/qiuxiang/react-native-amap-geolocation)


### 3.3.7 Camera 摄像头

1. 组件使用

    - 安装

        ```sh
        npm i react-native-vision-camera
        ```
    - 配置
        - [https://github.com/mrousavy/react-native-vision-camera](https://github.com/mrousavy/react-native-vision-camera)
        - [https://react-native-vision-camera.com/docs/guides](https://react-native-vision-camera.com/docs/guides)

    - 使用

        拍照、扫码、人脸识别......

2. 代码示例

    ```tsx
    import React, { useState, useRef, useEffect } from 'react';
    import {
        View,
        Text,
        TouchableOpacity,
        StyleSheet,
        Image,
        Alert,
    } from 'react-native';
    import { Camera, PhotoFile, useCameraDevice } from 'react-native-vision-camera';

    const CameraApp: React.FC = () => {
    const camera = useRef<Camera>(null);
    const device = useCameraDevice('back');
    const [hasPermission, setHasPermission] = useState<boolean>(false);
    const [photo, setPhoto] = useState<PhotoFile | null>(null);

    // 请求相机权限
    useEffect(() => {
        const requestCameraPermission = async () => {
        const permission = await Camera.requestCameraPermission();
        console.log(permission);
        Alert.alert(permission)
        
        setHasPermission(permission === 'granted');
        };
        requestCameraPermission();
    }, []);

    // 拍照函数
    const takePhoto = async () => {
        if (camera.current) {
        try {
            const currentPhoto = await camera.current.takePhoto();
            setPhoto(currentPhoto);
        } catch (error) {
            Alert.alert('拍照失败');
        }
        }
    };

    // 重拍
    const retakePhoto = () => {
        setPhoto(null);
    };

    // 如果没有权限
    if (!hasPermission) {
        return (
        <View style={styles.container}>
            <Text style={styles.text}>需要相机权限</Text>
        </View>
        );
    }

    // 如果没有设备
    if (!device) {
        return (
        <View style={styles.container}>
            <Text style={styles.text}>没有找到相机设备</Text>
        </View>
        );
    }

    // 如果拍了照片，显示预览
    if (photo) {
        return (
            <View style={styles.container}>
                <Image 
                source={{ uri: `file://${photo.path}` }} 
                style={StyleSheet.absoluteFill}
                />
                <View style={styles.previewControls}>
                <TouchableOpacity style={styles.button} onPress={retakePhoto}>
                    <Text style={styles.buttonText}>重拍</Text>
                </TouchableOpacity>
                </View>
            </View>
        );
    }

    // 主相机界面
    return (
        <View style={styles.container}>
            <Camera
                ref={camera}
                style={StyleSheet.absoluteFill}
                device={device}
                isActive={true}
                photo={true}
            />
            
            <TouchableOpacity style={styles.captureButton} onPress={takePhoto}>
                <View style={styles.captureInner} />
            </TouchableOpacity>
        </View>
    );
    };

    const styles = StyleSheet.create({
        container: {
            flex: 1,
            backgroundColor: 'black',
        },
        text: {
            color: 'white',
            fontSize: 18,
            textAlign: 'center',
            marginTop: 100,
        },
        captureButton: {
            position: 'absolute',
            bottom: 50,
            alignSelf: 'center',
            width: 70,
            height: 70,
            borderRadius: 35,
            backgroundColor: 'white',
            justifyContent: 'center',
            alignItems: 'center',
        },
        captureInner: {
            width: 60,
            height: 60,
            borderRadius: 30,
            backgroundColor: 'white',
            borderWidth: 2,
            borderColor: 'black',
        },
        previewControls: {
            position: 'absolute',
            bottom: 50,
            alignSelf: 'center',
        },
        button: {
            backgroundColor: 'rgba(255,255,255,0.3)',
            paddingHorizontal: 30,
            paddingVertical: 15,
            borderRadius: 25,
        },
        buttonText: {
            color: 'white',
            fontSize: 16,
            fontWeight: 'bold',
        },
    });

    export default CameraApp;
    ```

3. 运行结果

    ![](/application/react-native/037.png)

### 3.3.8 image-picker 图像相关

1. 组件使用

    - 安装

        ```sh
        npm install react-native-image-picker
        ```
    - 配置
        - [https://github.com/react-native-image-picker/react-native-image-picker](https://github.com/react-native-image-picker/react-native-image-picker)
        - 
    - 使用
        - 调用摄像头
        - 访问相册

2. 代码示例

    ```tsx
    import React, { useState } from 'react';
    import {
        View,
        Text,
        TouchableOpacity,
        StyleSheet,
        Image,
        Alert,
        ActivityIndicator,
    } from 'react-native';
    import {
        launchCamera,
        launchImageLibrary,
        CameraOptions,
        ImageLibraryOptions,
        ImagePickerResponse,
    } from 'react-native-image-picker';

    interface AvatarPickerProps {
        onAvatarSelected?: (avatarUri: string) => void;
        initialAvatar?: string;
        size?: number;
    }

    const AvatarPicker: React.FC<AvatarPickerProps> = ({
        onAvatarSelected,
        initialAvatar,
        size = 120,
    }) => {
        const [avatarUri, setAvatarUri] = useState<string | null>(initialAvatar || null);
        const [loading, setLoading] = useState<boolean>(false);

        // 头像配置 - 专注于头像使用的优化配置
        const cameraOptions: CameraOptions = {
            mediaType: 'photo',
            includeBase64: false,
            maxHeight: 800,  // 适合头像的尺寸
            maxWidth: 800,
            quality: 0.7,    // 适中的质量
            saveToPhotos: false, // 头像不需要保存到相册
            cameraType: 'front', // 头像通常用前置摄像头
        };

        const libraryOptions: ImageLibraryOptions = {
            mediaType: 'photo',
            includeBase64: false,
            maxHeight: 800,
            maxWidth: 800,
            quality: 0.7,
            selectionLimit: 1, // 头像只能选一张
        };

        // 处理选择结果
        const handleResponse = (response: ImagePickerResponse) => {
            setLoading(false);

            if (response.didCancel) {
                return;
            }

            if (response.errorCode) {
                let errorMessage = '选择失败';
                
                switch (response.errorCode) {
                    case 'camera_unavailable':
                    errorMessage = '相机不可用';
                    break;
                    case 'permission':
                    errorMessage = '没有相机或相册权限';
                    break;
                }

                Alert.alert('错误', errorMessage);
                return;
            }

            if (response.assets && response.assets.length > 0) {
                const asset = response.assets[0];
                if (asset.uri) {
                    setAvatarUri(asset.uri);
                    onAvatarSelected?.(asset.uri);
                    Alert.alert('成功', '头像已更新');
                }
            }
        };

        // 打开相机拍照
        const takePhoto = () => {
            setLoading(true);
            launchCamera(cameraOptions, handleResponse);
        };

        // 打开相册选择
        const chooseFromGallery = () => {
            setLoading(true);
            launchImageLibrary(libraryOptions, handleResponse);
        };

        // 删除头像
        const removeAvatar = () => {
            Alert.alert(
                '删除头像',
                '确定要删除当前头像吗？',
                [
                    { text: '取消', style: 'cancel' },
                    { 
                        text: '删除', 
                        style: 'destructive',
                        onPress: () => {
                            setAvatarUri(null);
                            onAvatarSelected?.('');
                        }
                    },
                ]
            );
        };

        // 显示选择选项
        const showAvatarOptions = () => {
            Alert.alert(
                '选择头像',
                '请选择获取头像的方式',
                [
                    { text: '取消', style: 'cancel' },
                    { text: '拍照', onPress: takePhoto },
                    { text: '从相册选择', onPress: chooseFromGallery },
                    avatarUri ? { text: '删除头像', style: 'destructive', onPress: removeAvatar } : null,
                ].filter(Boolean) as any
            );
        };

        const avatarSize = {
            width: size,
            height: size,
            borderRadius: size / 2,
        };

        return (
            <View style={styles.container}>
                <TouchableOpacity 
                    style={[styles.avatarContainer, avatarSize]}
                    onPress={showAvatarOptions}
                    disabled={loading}
                >
                    {loading ? (
                        <View style={[styles.loadingContainer, avatarSize]}>
                            <ActivityIndicator size="small" color="#007AFF" />
                        </View>
                        ) : avatarUri ? (
                        <Image 
                            source={{ uri: avatarUri }} 
                            style={[styles.avatarImage, avatarSize]}
                            resizeMode="cover"
                        />
                        ) : (
                        <View style={[styles.placeholder, avatarSize]}>
                            <Text style={styles.placeholderText}>点击选择头像</Text>
                        </View>
                    )}
                    
                    {/* 编辑图标 */}
                    <View style={styles.editIcon}>
                        <Text style={styles.editIconText}>✎</Text>
                    </View>
                </TouchableOpacity>

                <Text style={styles.hintText}>
                    点击头像可以拍照或从相册选择
                </Text>
            </View>
        );
    };

    const styles = StyleSheet.create({
        container: {
            alignItems: 'center',
            padding: 20,
        },
        avatarContainer: {
            backgroundColor: '#f0f0f0',
            justifyContent: 'center',
            alignItems: 'center',
            borderWidth: 3,
            borderColor: '#e0e0e0',
            position: 'relative',
            shadowColor: '#000',
            shadowOffset: {
            width: 0,
            height: 2,
            },
            shadowOpacity: 0.1,
            shadowRadius: 3.84,
            elevation: 5,
        },
        avatarImage: {
            backgroundColor: '#f0f0f0',
        },
        placeholder: {
            backgroundColor: '#e0e0e0',
            justifyContent: 'center',
            alignItems: 'center',
            borderWidth: 2,
            borderColor: '#ccc',
            borderStyle: 'dashed',
        },
        placeholderText: {
            color: '#666',
            fontSize: 12,
            textAlign: 'center',
            paddingHorizontal: 8,
        },
        loadingContainer: {
            backgroundColor: '#f8f8f8',
            justifyContent: 'center',
            alignItems: 'center',
        },
        editIcon: {
            position: 'absolute',
            bottom: 0,
            right: 0,
            backgroundColor: '#007AFF',
            width: 28,
            height: 28,
            borderRadius: 14,
            justifyContent: 'center',
            alignItems: 'center',
            borderWidth: 2,
            borderColor: 'white',
        },
        editIconText: {
            color: 'white',
            fontSize: 14,
            fontWeight: 'bold',
        },
        hintText: {
            marginTop: 12,
            fontSize: 14,
            color: '#666',
            textAlign: 'center',
        },
    });

    export default AvatarPicker;
    ```

3. 运行结果

    ![](/application/react-native/038.png)

## 3.4 自定义组件

1. 工程师自己写的组件

    - React 是面向组件开发的（所有内容都是组件）
    - 这里的自定义组件是指：具有特定功能的，可以重复使用的公共组件

2. 代码示例

    ```tsx
    import { StyleSheet, Text, View ,ActivityIndicator } from 'react-native'
    import React from 'react'

    export default function index() {
        return (
            <View style={[styles.container]}>
                <View style={[styles.loading]}>
                    <ActivityIndicator color={"white"}/>
                    <Text style={[styles.loadingTitle]}>加载中...</Text>
                </View>
            </View>
        )
    }

    const styles = StyleSheet.create({
        container:{
            flex:1,
            justifyContent:"center",
            alignItems:"center"
        },
        loading:{
            backgroundColor:'#999',
            height:100,
            width:150,
            borderRadius:20,
            padding:20
        },
        loadingTitle:{
            textAlign:"center",
            color:'white',
            marginTop:20
        }
    })
    ```

3. 运行结果

    ![](/application/react-native/039.png)