# 一、处理系统的npm插件

## 解决无法删除node_modules的问题

```sh
# 使用 rimraf（Node.js 工具）
npx rimraf node_modules
# 或先安装
npm install -g rimraf
rimraf node_modules

# 使用 nuke（更强大）
npx npkill
# 然后选择要删除的 node_modules
```
