# 一、认识 solid start

## 1.1 命令创建

1. 创建项目

    ```bash
    # 创建项目
    npm init solid
    ```

    ![](/framework/solid/solidStart/001.png)

2. 运行项目

    ```sh 
    cd 01-project & pnpm install
    npm run dev
    ```
3. 运行结果

    ![](/framework/solid/solidStart/002.png)


## 1.2 项目结构

```txt
my-solidstart-app/
├── public/                     # 静态资源（直接复制到输出目录）
│   ├── favicon.ico
│   └── robots.txt
│
├── src/
│   ├── routes/                 # 核心：文件系统路由（自动生成 URL 路由）
│   │   ├── api/                # API 路由（服务端函数）
│   │   │   ├── hello.ts
│   │   │   └── users/[id].ts
│   │   │
│   │   ├── (group)/            # 路由分组（可选，不影响 URL）
│   │   │   └── admin/
│   │   │       ├── layout.tsx
│   │   │       └── dashboard.tsx
│   │   │
│   │   ├── [param].tsx         # 动态路由（如 /user/123）
│   │   ├── [...spread].tsx     # 通配符路由（如 /files/a/b/c）
│   │   ├── index.tsx           # 根路径 → /
│   │   ├── about.tsx           # → /about
│   │   └── error.tsx           # 全局错误边界
│   │
│   ├── components/             # 可复用组件（非路由组件）
│   │   └── Header.tsx
│   │
│   ├── lib/                    # 工具函数、业务逻辑
│   │   └── auth.ts
│   │
│   ├── styles/                 # 全局样式
│   │   └── global.css
│   │
│   ├── entry-client.tsx        # 客户端入口（hydration）
│   ├── entry-server.tsx        # 服务端入口（SSR 渲染）
│   └── root.tsx                # 应用根组件（包含 Router、Provider 等）
│
├── .solid/                     # 构建产物（自动生成，勿提交）
│   └── start/
│
├── server/                     # 自定义服务器逻辑（可选）
│   └── index.ts
│
├── package.json
├── tsconfig.json
├── vite.config.ts              # Vite 配置（含 SolidStart 插件）
└── middleware.ts               # 全局中间件（可选）
```

## 1.3 hello world

1. 修改示例代码

    ```jsx
    import { createSignal } from "solid-js";

    export default function Home() {

      const [name,setName] = createSignal("world")

      return (
        <main>
          <h1>Hello {name()}!</h1>
        </main>
      );
    }
    ```

2. 运行结果

    ![](/framework/solid/solidStart/003.png)